// Legacy alias — the marketing footer implementation now lives in
// `@/components/footers/MarketingFooter` (responsive, with collapsible
// mobile sections). Kept so existing `Footerdemo` imports keep working.
export { MarketingFooter as Footerdemo } from "@/components/footers/MarketingFooter";
